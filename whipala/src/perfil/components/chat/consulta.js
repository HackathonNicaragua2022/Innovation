import React from 'react';

import {
    View,
    Text,
    Image,
    StyleSheet,
    ScrollView
} from 'react-native';

const App = () => {
    return(
        <ScrollView>
            <Text style={{color:'red'}}>sdocimldskimdsc</Text>
        </ScrollView>
    )
}

export default App;